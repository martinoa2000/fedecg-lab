# Phase 5: FedProx across 10 hospitals with Dirichlet(0.3) label skew.

extends: federated.yaml

experiment: {phase: 5, setting: "FedProx, label skew (alpha 0.3)"}

federated:
  partition: dirichlet
  n_clients: 10
  dirichlet_alpha: 0.3
  min_client_records: 300
  strategy: fedprox
