# Baseline tuning: the recipe before tuning, scored on validation only.
# The reference every other tune_*.yaml is compared with.

extends: tune_base.yaml

experiment: {setting: "Before tuning (32 channels, crops only)"}
