# Baseline tuning: gain, noise, baseline wander and lead dropout on top of crops.

extends: centralized.yaml

experiment: {setting: "More augmentation"}

training:
  augment: {amplitude: 0.1, noise: 0.05, wander: 0.1, lead_dropout: 0.1}
