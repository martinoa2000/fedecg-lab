# Phase 5: FedProx with one hospital per ECG recording device.
# Devices with at least 500 training records each become a hospital; the rest
# are pooled.

extends: federated.yaml

experiment: {phase: 5, setting: "FedProx, by device"}

federated:
  partition: device
  strategy: fedprox
